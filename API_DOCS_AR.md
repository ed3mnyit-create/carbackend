# توثيق واجهة برمجة تطبيقات منصة C4R

مرحباً بك في التوثيق الشامل لواجهة برمجة تطبيقات (API) الخاصة بمنصة C4R لتأجير السيارات. يغطي هذا المستند جميع الروابط (Endpoints) المتاحة، الطرق المستخدمة (Methods)، وتفاصيل الاستخدام.

## 🔗 الرابط الأساسي (Base URL)
```
https://c4r-platform-backend.vercel.app/api
```
*(أثناء التطوير المحلي، يمكنك استخدام `http://localhost:5000/api`)*

---

## 🛡️ المصادقة والتفويض (Authentication & Authorization)
معظم الروابط تتطلب تسجيل دخول ومصادقة. يجب تقديم رمز JWT صالح في رأس الطلب (Header):
`Authorization: Bearer <your_jwt_token>`

بعض الروابط مخصصة للمسؤولين فقط باستخدام وسيط (`isAdmin`).

---

## 🚦 النظام والصحة (System & Health)
روابط للتحقق من حالة النظام والخادم.

| الطريقة | الرابط (Endpoint) | الوصف | الصلاحية |
|---|---|---|---|
| GET | `/health` | التحقق مما إذا كان الخادم يعمل وبحالة جيدة. | عام |
| GET | `/` | رسالة الترحيب الخاصة بالجذر. | عام |
| GET | `/api` | رسالة الترحيب الخاصة بأساس الواجهة. | عام |

---

## 🔐 المصادقة (`/api/auth`)
إدارة تسجيل المستخدمين، تسجيل الدخول، وإدارة كلمات المرور.

| الطريقة | الرابط (Endpoint) | الوصف | الصلاحية |
|---|---|---|---|
| POST | `/api/auth/register` | تسجيل حساب مستخدم جديد. | عام |
| POST | `/api/auth/login` | تسجيل الدخول والحصول على رمز JWT. | عام |
| POST | `/api/auth/forgot-password` | طلب رابط لإعادة تعيين كلمة المرور. | عام |
| POST | `/api/auth/reset-password` | إعادة تعيين كلمة المرور باستخدام رمز صالح. | عام |
| PUT | `/api/auth/change-password` | تغيير كلمة المرور للمستخدم المسجل حالياً. | خاص |
| GET | `/api/auth/logout` | تسجيل الخروج وحذف ملفات تعريف الارتباط (Cookies). | خاص |

---

## 👤 المستخدمون (`/api/users`)
إدارة ملفات تعريف المستخدمين والحسابات.

| الطريقة | الرابط (Endpoint) | الوصف | الصلاحية |
|---|---|---|---|
| GET | `/api/users/` | الحصول على قائمة بجميع المستخدمين المسجلين. | مسؤول |
| GET | `/api/users/profile` | الحصول على الملف الشخصي للمستخدم الحالي. | خاص |
| PUT | `/api/users/profile` | تحديث الملف الشخصي للمستخدم الحالي. | خاص |
| PUT | `/api/users/:id` | تحديث تفاصيل مستخدم معين. | مسؤول |
| DELETE| `/api/users/:id` | حذف حساب مستخدم. | مسؤول |

---

## 🌍 المناطق (`/api/regions`)
إدارة المناطق والمدن المتاحة لتأجير السيارات.

| الطريقة | الرابط (Endpoint) | الوصف | الصلاحية |
|---|---|---|---|
| GET | `/api/regions/` | الحصول على جميع المناطق المتاحة. | عام |

---

## 🚗 السيارات (`/api/cars`)
إدارة أسطول السيارات المتاح للتأجير.

| الطريقة | الرابط (Endpoint) | الوصف | الصلاحية |
|---|---|---|---|
| GET | `/api/cars/` | الحصول على جميع السيارات (يدعم الفلترة، التقسيم، والبحث). | عام |
| GET | `/api/cars/:id` | الحصول على تفاصيل سيارة معينة باستخدام المعرف (ID). | عام |
| POST | `/api/cars/` | إضافة سيارة جديدة إلى الأسطول. | مسؤول |
| PUT | `/api/cars/:id` | تحديث تفاصيل سيارة موجودة. | مسؤول |
| DELETE| `/api/cars/:id` | حذف سيارة من الأسطول. | مسؤول |

---

## 📅 الحجوزات (`/api/bookings`)
التعامل مع طلبات الحجز وحالاتها.

| الطريقة | الرابط (Endpoint) | الوصف | الصلاحية |
|---|---|---|---|
| POST | `/api/bookings/` | إنشاء طلب حجز جديد. | خاص |
| GET | `/api/bookings/` | الحصول على الحجوزات (المستخدم يرى حجوزاته؛ المسؤول يرى الجميع). | خاص |
| GET | `/api/bookings/:id` | الحصول على تفاصيل حجز معين. | خاص |
| PATCH | `/api/bookings/:id/approve` | الموافقة على طلب حجز معلق. | مسؤول |
| PATCH | `/api/bookings/:id/reject` | رفض طلب حجز معلق. | مسؤول |
| DELETE| `/api/bookings/:id` | حذف حجز. | مسؤول |

---

## ⭐ التقييمات (`/api/reviews`)
إدارة تقييمات العملاء للسيارات.

| الطريقة | الرابط (Endpoint) | الوصف | الصلاحية |
|---|---|---|---|
| GET | `/api/reviews/car/:carId` | الحصول على جميع التقييمات المعتمدة لسيارة معينة. | عام |
| POST | `/api/reviews/` | تقديم تقييم جديد لسيارة. | خاص |
| GET | `/api/reviews/user` | الحصول على جميع التقييمات الخاصة بالمستخدم المسجل حالياً. | خاص |
| GET | `/api/reviews/recent` | الحصول على أحدث التقييمات. | عام |
| GET | `/api/reviews/admin/all` | الحصول على جميع التقييمات لإدارتها (بما فيها غير المعتمدة). | مسؤول |
| DELETE| `/api/reviews/:id` | حذف تقييم. | مسؤول |

---

## 📊 لوحة تحكم المسؤول (`/api/admin`)
روابط خاصة بالإحصائيات وتحليلات المنصة.

| الطريقة | الرابط (Endpoint) | الوصف | الصلاحية |
|---|---|---|---|
| GET | `/api/admin/stats` | الحصول على إحصائيات المنصة الشاملة (العدد الإجمالي، الإيرادات). | مسؤول |

---

## 🔔 الإشعارات (`/api/notifications`)
نظام الإشعارات المدمج داخل التطبيق للمستخدمين والمسؤولين.

| الطريقة | الرابط (Endpoint) | الوصف | الصلاحية |
|---|---|---|---|
| GET | `/api/notifications/` | الحصول على جميع الإشعارات للمستخدم الحالي. | خاص |
| POST | `/api/notifications/` | إنشاء إشعار جديد (نظام / مسؤول). | خاص |
| PATCH | `/api/notifications/mark-all-read`| تحديد جميع الإشعارات كمقروءة للمستخدم. | خاص |
| PATCH | `/api/notifications/:id/read` | تحديد إشعار معين كمقروء. | خاص |
| DELETE| `/api/notifications/:id` | حذف إشعار معين. | خاص |

---

## 📁 الفئات (`/api/categories`)
إدارة تصنيفات السيارات (مثلاً: عائلية، سيدان، فارهة).

| الطريقة | الرابط (Endpoint) | الوصف | الصلاحية |
|---|---|---|---|
| GET | `/api/categories/` | الحصول على جميع فئات السيارات. | عام |
| POST | `/api/categories/` | إنشاء فئة سيارات جديدة. | مسؤول |
| GET | `/api/categories/:id` | الحصول على فئة معينة باستخدام المعرف. | عام |
| PUT | `/api/categories/:id` | تحديث فئة معينة. | مسؤول |
| DELETE| `/api/categories/:id` | حذف فئة معينة. | مسؤول |

---

## 📝 المدونة (`/api/blogs`)
إدارة المحتوى والمقالات وأخبار المنصة.

| الطريقة | الرابط (Endpoint) | الوصف | الصلاحية |
|---|---|---|---|
| GET | `/api/blogs/` | الحصول على جميع المقالات المنشورة. | عام |
| POST | `/api/blogs/` | إنشاء مقال جديد. | مسؤول |
| GET | `/api/blogs/:id` | الحصول على مقال معين باستخدام المعرف (ID) أو الرابط (Slug). | عام |
| GET | `/api/blogs/:id/related` | الحصول على المقالات ذات الصلة. | عام |
| PUT | `/api/blogs/:id` | تحديث مقال معين. | مسؤول |
| DELETE| `/api/blogs/:id` | حذف مقال معين. | مسؤول |

---

## ⚙️ الإعدادات (`/api/settings`)
الإعدادات الشاملة للمنصة (مثل SEO، وسائل التواصل الاجتماعي).

| الطريقة | الرابط (Endpoint) | الوصف | الصلاحية |
|---|---|---|---|
| GET | `/api/settings/` | الحصول على جميع الإعدادات العامة. | عام |
| GET | `/api/settings/:key` | الحصول على إعداد معين باستخدام مفتاحه المميز. | عام |
| POST | `/api/settings/` | إنشاء أو تحديث إعداد معين. | مسؤول |
| DELETE| `/api/settings/:key` | حذف إعداد معين. | مسؤول |

---

## 🎫 العروض الترويجية (`/api/promos`)
إدارة أكواد الخصم واللافتات الترويجية.

| الطريقة | الرابط (Endpoint) | الوصف | الصلاحية |
|---|---|---|---|
| GET | `/api/promos/` | الحصول على جميع العروض الفعالة والعامة. | عام |
| GET | `/api/promos/admin` | الحصول على جميع العروض (بما فيها المنتهية/الغير فعالة). | مسؤول |
| POST | `/api/promos/` | إنشاء عرض ترويجي أو لافتة جديدة. | مسؤول |
| PUT | `/api/promos/:id` | تحديث عرض ترويجي موجود. | مسؤول |
| DELETE| `/api/promos/:id` | حذف عرض ترويجي. | مسؤول |

---

## 📝 ردود الأخطاء العامة (General Error Responses)
في حالة حدوث أخطاء، ستستجيب الواجهة برموز أخطاء HTTP القياسية وحمولة JSON:

```json
{
  "success": false,
  "message": "رسالة خطأ تفصيلية"
}
```

- **400 Bad Request:** بيانات مفقودة أو خطأ في التحقق.
- **401 Unauthorized:** الرمز مفقود أو غير صالح.
- **403 Forbidden:** الرمز صالح، ولكن لا يملك صلاحية المسؤول.
- **404 Not Found:** المورد غير موجود.
- **500 Internal Server Error:** خطأ من جهة الخادم (Server).
